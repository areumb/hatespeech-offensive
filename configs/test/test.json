{
  "pipeline": {
    "seed": "{seed}",
    "batch_size": 16,
    "logging_freq": 100,
    "output_predictions": "results/test/hatecheck/hate-clean-on-notclean-clean/SEED{seed}.json"
  },
  "task": {
    "model_name": "RoBERTa-base",
    "dataset_name": "HateCheck",
    "dataset_directory": "dataset/extended_hatecheck",
    "num_labels": 2,
    "checkpoint": "outputs/davidson/RoBERTa-base/hate-clean/best/seed{seed}_RoBERTa-base_{seed}.pt"
  },
  "processing": {
    "padding": false
  },
  "wandb": {
    "project_name": "hs-generalization-testset-on-hatecheck",
    "run_name": "hatecheck-hate-clean-on-notclean-clean_{seed}"
  }
}